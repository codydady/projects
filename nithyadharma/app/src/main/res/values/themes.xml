<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

<style name="Theme.NithyaDharma" parent="android:Theme.Material.Light.NoActionBar">
    <!-- Applies to ALL Android versions (API 26+) -->
    <item name="android:windowBackground">@android:color/black</item>

    <!-- Tells Android Studio: Only apply these on Android 12+ (API 31+) -->
    <item name="android:windowSplashScreenBackground" tools:targetApi="s">@android:color/black</item>
    <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="s">@android:color/transparent</item>
</style>

</resources>